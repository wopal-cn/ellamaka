长期记忆让代理能够跨会话保留稳定偏好、项目约定和重要工作上下文。

## 两层配置

- **全局配置**：所有空间默认继承
- **空间配置**：可以继承、独立覆盖或显式关闭

选择“继承全局”只会清除当前空间的覆盖项，不会修改全局开关和凭据。

## 模型职责

- LLM 用于整理、摘要和蒸馏记忆
- Embedding 模型用于语义检索

> API Key 只保存在对应配置文件中，不会在界面中回显。

**预计耗时：** 约 30 秒
