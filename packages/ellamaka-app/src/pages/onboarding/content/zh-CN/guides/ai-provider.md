配置 AI Provider 后，可以在 Ellamaka 中使用对应服务提供的模型。

## 配置流程

1. 在 Provider 控制台创建 API Key
2. 将 Key 粘贴到右侧输入框
3. 保存后由 Ellamaka 验证并安全存储

API Key 不会显示在结果页面，也不会写入日志。

> 这是可选步骤。跳过后仍可使用当前环境中已经可用的模型。

**预计耗时：** 约 2–3 分钟
